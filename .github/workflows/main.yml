name: github-action-bohlen

on:
  push:
    branches:
      - main

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Print Hello World
        run: echo "Hello, World!"
