name: List S3 Buckets Bohlen

on:
  workflow_dispatch:

jobs:
  list-s3-buckets:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up AWS CLI
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AKIA2UC3CW7MO5Q3MBLU }}
          aws-secret-access-key: ${{ secrets.JAp9TM1xJ9RrQdGhz3pHCAHSbMOfU+2gFFM1310g }}
          aws-region: ${{ secrets.us-east-2 }}

      - name: List S3 Buckets
        run: aws s3 ls
